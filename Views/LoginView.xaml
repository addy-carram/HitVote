<UserControl x:Class="HitVote.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1440">

    <Grid>
        <StackPanel x:Name="loginPanel" 
                    Margin="485,67,0,67" 
                    Background="{DynamicResource BackgroundCard}" 
                    HorizontalAlignment="Left" 
                    Width="400">
            <Image Height="64" Width="64" 
                   Source="/Views/logo.png" 
                   Margin="0,44,0,0"/>
            <StackPanel Width="212" Margin="40,30,40,15" Height="314">
                <StackPanel Margin="10,10,10,5">
                    <Label Content="Name" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBox x:Name="inputName" 
                             Background="{DynamicResource Inputs}" 
                             BorderBrush="{DynamicResource BorderColor}" 
                             Foreground="#FFE8C5" 
                             Height="40"
                             Margin="3,0,0,0" 
                             Padding="10"/>
                </StackPanel>
                <StackPanel Margin="10,5,10,5">
                    <Label Content="Password" Foreground="{DynamicResource TextPrimary}"/>
                    <PasswordBox x:Name="inputPassword" 
                                 Background="{DynamicResource Inputs}" 
                                 BorderBrush="{DynamicResource BorderColor}"
                                 Height="40" 
                                 Padding="10"/>
                </StackPanel>
                <StackPanel Margin="10,5,10,5">
                    <Label Content="Role" Foreground="{DynamicResource TextPrimary}"/>
                    <ComboBox x:Name="comboRole" Height="40" Padding="10"/>
                </StackPanel>
                <Button x:Name="enterLogin" 
                        Content="Enter" 
                        Background="{DynamicResource PrimaryColor}" 
                        Foreground="{DynamicResource TextPrimary}"
                        Height="40"
                        Margin="0,39,0,0"/>
            </StackPanel>
            <Label Content="Don't have an account? Sign in" 
                   Foreground="{DynamicResource TextPrimary}" 
                   Margin="40,0,40,40" 
                   HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>