<Window x:Class="HitVote.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HitVote.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="700
        " Width="1440"
        WindowStartupLocation="CenterScreen">

    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1215"/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="loginPanel" Margin="485,67,0,67" Background="{DynamicResource BackgroundCard}" HorizontalAlignment="Left" Width="400">
            <Image Height="64" Width="64" Source="/Views/logo.png" UseLayoutRounding="True" Margin="0,44,0,0"/>
            <StackPanel Width="212" Margin="40,30,40,15" Height="314">
                <StackPanel VerticalAlignment="Top" Margin="10,10,10,5">
                    <Label Content="Name" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Left"/>
                    <TextBox x:Name="inputName" TextWrapping="Wrap" Background="{DynamicResource Inputs}" BorderBrush="{DynamicResource BorderColor}" Foreground="#4CFFE8C5" Height="40" TextChanged="TextBox_TextChanged" Margin="3,0,0,0" Padding="10,10,5,10" AutomationProperties.IsRequiredForForm="True"
                             />
                </StackPanel>
                <StackPanel Margin="10,5,10,4.3">
                    <Label Content="Password" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Left"/>
                    <PasswordBox x:Name="inputPassword" Background="{DynamicResource Inputs}" Height="40" AutomationProperties.IsRequiredForForm="True" Margin="0,0,0,-2" Padding="10,10,10,10" BorderBrush="{DynamicResource BorderColor}"/>
                </StackPanel>
                <StackPanel Margin="10,5,10,5">
                    <Label Content="Role" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Left"/>
                    <ComboBox x:Name="comboRole" Height="40" Padding="10,10,10,10" Background="{x:Null}">
                        <ComboBoxItem Content="admin" Background="#FFDB8484"/>
                        <ComboBoxItem Content="user"/>
                    </ComboBox>
                </StackPanel>

                <Button x:Name="enterLogin" Content="Enter" Background="{DynamicResource PrimaryColor}" Padding="10,10,10,10" Margin="0,39,0,0" Foreground="{DynamicResource TextPrimary}"/>
            </StackPanel>
            <Label Content="Don't have an account? Sign in" Foreground="{DynamicResource TextPrimary}" Margin="40,0,40,40" HorizontalAlignment="Center"/>
        </StackPanel>




    </Grid>
</Window>
